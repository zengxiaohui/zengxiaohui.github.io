eq相等 ne、neq不相等， 
gt大于， lt小于 
gt大于， lt小于 
gte、ge大于等于   
lte、le 小于等于   
not非   mod求模   
is [not] div by是否能被某数整除   
is [not] even是否为偶数   
is [not] even by $b即($a / $b) % 2 == 0   
is [not] odd是否为奇   
is not odd by $b即($a / $b) % 2 != 0 

<c:forEach items="${pathsPin }" var="pin" varStatus="stat">
    <c:if test="${!stat.last }">${pin } → </c:if>
    <c:if test="${stat.last }">${pin } </c:if>
</c:forEach>